// This file is intentionally small.
// The old frontend demo duplicated admin logic across several files.
// Real admin behavior now lives in admin.js and admin-features.js.

document.addEventListener('DOMContentLoaded', () => {
    window.lucide?.createIcons();
});
